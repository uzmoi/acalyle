query BookDetail($bookId: ID!) {
  book(id: $bookId) {
    createdAt
    tags
    # tagMetaDatum(symbol: "") {}
    # tagMetaData {
    #   symbol
    #   props
    #   description
    # }
    templates: tagProps(symbol: "@template")
    # resources
    # settings {
    #   extensions
    #   implicitQuery
    # }
  }
}
