/* eslint-disable pure-module/pure-module */

import { varName } from "@acalyle/ui";
import { defaultTheme } from "../theme";

export const lightBg = "#F7F9F2"; // <- Storybook default
export const darkBg = defaultTheme[varName("app-bg")]!;
