#import BooksPage from "./books-page.graphql"

query BackwardBookPagination($count: Int!, $cursor: String, $query: String!) {
  books(last: $count, before: $cursor, query: $query) {
    ...BooksPage
  }
}
