{
  "private": true,
  "name": "@acalyle/eslint-config",
  "type": "module",
  "sideEffects": false,
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --minify-syntax --clean",
    "check": "tsc",
    "test": "vitest"
  },
  "devDependencies": {
    "@acalyle/tsconfig": "*",
    "tsup": "^6.7.0",
    "vitest": "^0.30.1"
  }
}
