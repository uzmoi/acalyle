{
  "private": true,
  "name": "@acalyle/idb",
  "type": "module",
  "sideEffects": false,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsup",
    "check": "tsc",
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "emnorst": "^1.0.0-next.2"
  },
  "devDependencies": {
    "@acalyle/tsconfig": "*",
    "@vitest/coverage-c8": "^0.30.1",
    "tsup": "^7.1.0",
    "vitest": "^0.30.1"
  }
}
